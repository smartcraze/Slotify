apiVersion: v1
kind: Service
metadata:
  name: slotify-service
  labels:
    app: slotify
spec:
  type: ClusterIP # Change to LoadBalancer if you don't use an Ingress controller
  selector:
    app: slotify
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
